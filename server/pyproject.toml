[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.121.3",
    "pytest>=9.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.14.2",
    "ty>=0.0.17",
]
